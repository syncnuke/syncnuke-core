# Set root level
level = info

# Configure console writer
writer = console
writer.format = {date: yyyy-MM-dd HH:mm:ss} [{thread}] {level|min-size=5} {class-name} - {message}

# Exception handling
exception = true
